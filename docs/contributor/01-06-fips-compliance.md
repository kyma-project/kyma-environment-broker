# FIPS 140-3 Compliance

Kyma Environment Broker image is built with the **GOFIPS140=v1.0.0** environment variable and run with the **GODEBUG=fips140=only** environment variable, therefore the broker runs in FIPS 140-3 mode. 