apiVersion: v1
kind: Service
metadata:
  name: website
  labels:
    app.kubernetes.io/name: website
spec:
  type: ClusterIP
  ports:
    - port: 4180
      targetPort: oauth2-proxy
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: website
