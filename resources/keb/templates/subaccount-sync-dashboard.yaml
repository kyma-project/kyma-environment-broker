apiVersion: v1
kind: ConfigMap
metadata:
  name: subaccount-sync-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
    app: monitoring-grafana
  annotations:
    argocd.argoproj.io/sync-options: Prune=false
data:
  subaccount-sync-dashboard.json: |-
{{ .Files.Get "files/subaccount-sync-dashboard.json" | indent 4 }}